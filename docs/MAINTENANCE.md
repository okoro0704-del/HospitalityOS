# Maintenance

Lightweight tickets on assets — not a full CMMS.

## Record

Issue, priority, status, assignee, notes, created/resolved timestamps.

## Statuses

`open` → `in_progress` → `resolved` → `closed`

Opening a ticket sets the asset to `maintenance`; resolving returns it to `available`.

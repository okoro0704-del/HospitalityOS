# Operational Tasks

Generic cross-vertical work items.

## Fields

Title, description, priority, assignee, due date, status, optional `relatedEntityType` / `relatedEntityId`, comments.

## Statuses

`open`, `assigned`, `in_progress`, `completed`, `cancelled`

Vertical modules can attach tasks to rooms, screens, venues, etc. without owning a separate task engine.
